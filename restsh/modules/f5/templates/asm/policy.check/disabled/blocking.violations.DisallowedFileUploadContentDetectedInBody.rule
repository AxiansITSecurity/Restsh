#OK: "Disallowed file upload content detected in body" is in blocking.
#WARN: "Disallowed file upload content detected in body" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_FILE_UPLOAD_IN_BODY") |
        select(.block == false or .alarm == false)
