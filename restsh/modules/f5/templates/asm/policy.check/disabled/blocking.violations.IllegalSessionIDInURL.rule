#OK: "Illegal session ID in URL" is in blocking.
#WARN: "Illegal session ID in URL" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_DYNAMIC_SESSION") |
        select(.block == false or .alarm == false)
