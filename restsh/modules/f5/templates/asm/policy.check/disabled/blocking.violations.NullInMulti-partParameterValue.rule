#OK: "Null in multi-part parameter value" is in blocking.
#WARN: "Null in multi-part parameter value" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_MULTIPART_NULL_VALUE") |
        select(.block == false or .alarm == false)
