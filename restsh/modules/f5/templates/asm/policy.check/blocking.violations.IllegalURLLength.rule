#OK: "Illegal URL length" is in blocking.
#WARN: "Illegal URL length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_URL_LENGTH") |
        select(.block == false or .alarm == false)
