#OK: "Illegal parameter value length" is in blocking.
#WARN: "Illegal parameter value length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_VALUE_LENGTH") |
        select(.block == false or .alarm == false)
