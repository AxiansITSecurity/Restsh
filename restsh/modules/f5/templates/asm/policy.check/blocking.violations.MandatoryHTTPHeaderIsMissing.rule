#OK: "Mandatory HTTP header is missing" is in blocking.
#WARN: "Mandatory HTTP header is missing" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_MANDATORY_HEADER") |
        select(.block == false or .alarm == false)
