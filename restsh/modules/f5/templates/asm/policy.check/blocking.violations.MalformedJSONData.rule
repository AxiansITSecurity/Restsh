#OK: "Malformed JSON data" is in blocking.
#WARN: "Malformed JSON data" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_JSON_MALFORMED") |
        select(.block == false or .alarm == false)
