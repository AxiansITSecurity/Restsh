#OK: "Illegal query string length" is in blocking.
#WARN: "Illegal query string length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_QUERY_STRING_LENGTH") |
        select(.block == false or .alarm == false)
