#!/usr/bin/env bash

# (c) Axians IT Security GmbH
# Jürgen Mang <juergen.mang@sec.axians.de>
# https://www.axians.de/security

# Shortdesc: Imports an OpenAPI file into a F5 ASM policy.
# Desc:
# Imports an OpenAPI file into a F5 ASM policy.

# Strict error handling
set -eEu -o pipefail

# Debug mode
[ -n "${RESTSH_DEBUG+x}" ] && set -x

if [ -z "${RESTSH_PATH+x}" ]
then
    echo "Script must be run in the restsh environment."
    exit 1
fi

APPLY_POLICY=1
while getopts ':hn' OPTION
do
    case "$OPTION" in
        n) APPLY_POLICY=0 ;;
        *) OPTION="invalid"; break ;;
    esac
done
shift "$((OPTIND -1))"

if [ "$OPTION" = "invalid" ] || [ -z "${1+x}" ] || [ -z "${2+x}" ]
then
    exec 1>&2
    _restsh.help.desc.get "$0"
    echo "Usage: $(basename "$0") [options...] <filename> <policy name>"
    echo "Options:"
    echo "    -n  Do not apply policy after import"
    exit 2
fi

restsh.util.check.string "filename" "$1"; FILENAME=$1
restsh.util.check.string "policy" "$2"; POLICY=$2

# Upload the openapi file
f5.file.upload "/mgmt/tm/asm/file-transfer/uploads" "$FILENAME"

FILENAME=$(basename "$FILENAME")
POLICY_HASH=$(f5.asm.policy.gethash "$POLICY")

# Start import swagger file task
TASK_ID=$($RESTSH_JQ -n --arg swagger_file "$FILENAME" \
       --arg waf_policy "/mgmt/tm/asm/policies/$POLICY_HASH" \
'{
    apiType: "swagger",
    filename: $swagger_file,
    policyReference: {
        link: $waf_policy
    }
}' | POST "/mgmt/tm/asm/tasks/import-open-api?\$select=id" | \
        $RESTSH_JQ -r ".id")

if [ -z "${TASK_ID}" ]
then
    echo_err "Importing swagger file \"$FILENAME\" failed"
    f5.file.remove "/var/ts/var/rest/$FILENAME"
    exit 1
fi

# Wait for completed task
if ! f5.asm.taskwait import-open-api "$TASK_ID"
then
    echo_err "Import of policy failed."
    f5.file.remove "/var/ts/var/rest/${RESTSH_USER}~$FILENAME"
    exit 1
fi

f5.file.remove "/var/ts/var/rest/${RESTSH_USER}~${FILENAME}"

# Apply policy
if [ "$APPLY_POLICY" -eq 1 ]
then
    f5.asm.policy.apply "$POLICY_HASH"
fi
