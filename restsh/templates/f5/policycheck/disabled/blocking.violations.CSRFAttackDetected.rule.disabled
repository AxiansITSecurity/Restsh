#OK: "CSRF attack detected" is in blocking.
#WARN: "CSRF attack detected" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_CSRF") |
        select(.block == false or .alarm == false)
