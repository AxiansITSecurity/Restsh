#OK: "Illegal meta character in value" is in blocking.
#WARN: "Illegal meta character in value" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_VALUE_METACHAR") |
        select(.block == false or .alarm == false)
