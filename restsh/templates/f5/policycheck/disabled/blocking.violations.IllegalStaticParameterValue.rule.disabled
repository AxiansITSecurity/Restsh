#OK: "Illegal static parameter value" is in blocking.
#WARN: "Illegal static parameter value" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_STATIC_VALUE") |
        select(.block == false or .alarm == false)
