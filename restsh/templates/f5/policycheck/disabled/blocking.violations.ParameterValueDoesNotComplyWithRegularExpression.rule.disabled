#OK: "Parameter value does not comply with regular expression" is in blocking.
#WARN: "Parameter value does not comply with regular expression" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_VALUE_REGEXP") |
        select(.block == false or .alarm == false)
