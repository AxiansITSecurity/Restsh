#OK: "Illegal cookie length" is in blocking.
#WARN: "Illegal cookie length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_COOKIE_LENGTH") |
        select(.block == false or .alarm == false)
