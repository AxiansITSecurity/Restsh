#OK: "Illegal parameter" is in blocking.
#WARN: "Illegal parameter" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER") |
        select(.block == false or .alarm == false)
