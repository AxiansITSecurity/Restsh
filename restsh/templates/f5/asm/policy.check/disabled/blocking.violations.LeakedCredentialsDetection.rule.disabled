#OK: "Leaked Credentials Detection" is in blocking.
#WARN: "Leaked Credentials Detection" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_LEAKED_CREDENTIALS") |
        select(.block == false or .alarm == false)
