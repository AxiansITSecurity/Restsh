#OK: "Virus detected" is in blocking.
#WARN: "Virus detected" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_VIRUS") |
        select(.block == false or .alarm == false)
