#OK: "Brute Force: Maximum login attempts are exceeded" is in blocking.
#WARN: "Brute Force: Maximum login attempts are exceeded" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_BRUTE_FORCE") |
        select(.block == false or .alarm == false)
