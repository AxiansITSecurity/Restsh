#OK: "Illegal flow to URL" is in blocking.
#WARN: "Illegal flow to URL" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_FLOW") |
        select(.block == false or .alarm == false)
