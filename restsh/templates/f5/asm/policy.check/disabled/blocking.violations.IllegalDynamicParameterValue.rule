#OK: "Illegal dynamic parameter value" is in blocking.
#WARN: "Illegal dynamic parameter value" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_PARAMETER_DYNAMIC_VALUE") |
        select(.block == false or .alarm == false)
