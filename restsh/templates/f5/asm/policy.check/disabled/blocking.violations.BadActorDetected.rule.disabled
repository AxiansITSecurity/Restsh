#OK: "Bad Actor Detected" is in blocking.
#WARN: "Bad Actor Detected" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_MALICIOUS_DEVICE") |
        select(.block == false or .alarm == false)
