#OK: "Illegal WebSocket frame length" is in blocking.
#WARN: "Illegal WebSocket frame length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_WEBSOCKET_FRAME_LENGTH") |
        select(.block == false or .alarm == false)
