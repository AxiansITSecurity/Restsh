#OK: "Threat Campaign detected" is in blocking.
#WARN: "Threat Campaign detected" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_THREAT_CAMPAIGN") |
        select(.block == false or .alarm == false)
