#OK: "Illegal WebSocket binary message length" is in blocking.
#WARN: "Illegal WebSocket binary message length" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_WEBSOCKET_BINARY_MESSAGE_LENGTH") |
        select(.block == false or .alarm == false)
