#OK: "Expired timestamp" is in blocking.
#WARN: "Expired timestamp" is not in block and alarm
.policy."blocking-settings".violations[] |
    select(.name == "VIOL_COOKIE_EXPIRED") |
        select(.block == false or .alarm == false)
